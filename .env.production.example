# Copia a .env en el servidor:  cp .env.production.example .env

# Contraseña de PostgreSQL (cámbiala)
POSTGRES_USER=game
POSTGRES_PASSWORD=cambia_esta_contraseña_segura
POSTGRES_DB=stonegrass

# Puerto HTTP público (80 en EC2; abre el puerto en el Security Group)
HTTP_PORT=80
